# Хостовая часть: только то, что нужно agent.py.
# Все RE-инструменты живут в контейнере, здесь их нет.
litellm>=1.60
python-dotenv>=1.0
# SSH к настоящей Raspberry Pi (нужен, только если она используется).
paramiko>=3.4
# targets.yaml для judge.py (этап 5).
PyYAML>=6.0
